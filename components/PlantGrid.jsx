import{useMemo,useState}from'react';import{Search}from'lucide-react';import{PlantCard}from'./PlantCard';import{categories}from'../data/plants';export function PlantGrid({plants,onAddToCart}){const[q,setQ]=useState(''),[cat,setCat]=useState('all'),[sort,setSort]=useState('featured');const list=useMemo(()=>plants.filter(p=>(cat==='all'||p.category===cat)&&(!q||`${p.name} ${p.id} ${p.category}`.toLowerCase().includes(q.toLowerCase()))).sort((a,b)=>sort==='price-low'?a.price-b.price:sort==='price-high'?b.price-a.price:sort==='name'?a.name.localeCompare(b.name):(b.featured?1:0)-(a.featured?1:0)),[plants,q,cat,sort]);return <div className="space-y-8"><div className="bg-white p-5 rounded-2xl border space-y-4"><div className="flex flex-col md:flex-row gap-4 justify-between"><div className="relative w-full md:w-96"><Search className="absolute left-3 top-3 w-5"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search plants or ID..." className="w-full pl-10 p-3 rounded-xl border bg-stone-50"/></div><select value={sort} onChange={e=>setSort(e.target.value)} className="p-3 rounded-xl border"><option value="featured">Featured</option><option value="price-low">Price: Low to High</option><option value="price-high">Price: High to Low</option><option value="name">Name A-Z</option></select></div><div className="flex gap-2 overflow-x-auto">{categories.map(c=><button key={c.id} onClick={()=>setCat(c.id)} className={`px-4 py-2 rounded-xl whitespace-nowrap ${cat===c.id?'bg-emerald-800 text-white':'bg-stone-100'}`}>{c.icon} {c.name}</button>)}</div></div>{list.length?<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">{list.map(p=><PlantCard key={p.id} plant={p} onAddToCart={onAddToCart}/>)}</div>:<div className="text-center py-16">No plants found.</div>}</div>}
