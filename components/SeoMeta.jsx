import {useEffect} from 'react';import {siteConfig} from '../config/site';export function SeoMeta({title,description}){useEffect(()=>{document.title=title?`${title} | ${siteConfig.name}`:siteConfig.seo.title;let m=document.querySelector('meta[name="description"]');if(!m){m=document.createElement('meta');m.name='description';document.head.appendChild(m)}m.content=description||siteConfig.seo.description},[title,description]);return null}
